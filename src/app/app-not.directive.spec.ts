import { AppNotDirective } from './app-not.directive';

describe('AppNotDirective', () => {
  it('should create an instance', () => {
    const directive = AppNotDirective;
    expect(directive).toBeTruthy();
  });
});
